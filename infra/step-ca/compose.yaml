services:
  step-ca:
    image: smallstep/step-ca:latest
    ports:
      - "9000:9000"
    environment:
      - TZ=America/Halifax
      - DOCKER_STEPCA_INIT_NAME=Homelab Step CA
      - DOCKER_STEPCA_INIT_DNS_NAMES=step-ca,stepca.homelab,localhost
      - DOCKER_STEPCA_INIT_PROVISIONER_NAME=stepca
      - DOCKER_STEPCA_INIT_PASSWORD_FILE=/run/secrets/step_ca_password
      - DOCKER_STEPCA_INIT_REMOTE_MANAGEMENT=false
      - DOCKER_STEPCA_INIT_ACME=true
    volumes:
      - /mnt/docker-data/infra/step-ca/certs:/home/step/certs
      - /mnt/docker-data/infra/step-ca/config:/home/step/config
      - /mnt/docker-data/infra/step-ca/db:/home/step/db
    secrets:
      - step_ca_password
    networks:
      - infra
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: '0.2'
          memory: 128M
    healthcheck:
      test: [ "CMD", "step", "ca", "health", "--ca-url", "https://localhost:9000" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 2m

networks:
  infra:
    external: true

secrets:
  step_ca_password:
    external: true
