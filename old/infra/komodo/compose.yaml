services:
  komodo:
    image: ghcr.io/moghtech/komodo-core:2
    environment:
      TZ: America/Halifax
      KOMODO_DATABASE_URI_FILE: /run/secrets/komodo_db_uri
      KOMODO_HOST: https://komodo.homelab
      KOMODO_TITLE: Homelab Komodo
      KOMODO_LOCAL_AUTH: "false"
      KOMODO_ENABLE_NEW_USERS: "false"
      KOMODO_DISABLE_CONFIRM_DIALOG: "false"
      KOMODO_PERIPHERY_PUBLIC_KEY: file:/config/keys/peripheries.pub
      KOMODO_WEBHOOK_SECRET_FILE: /run/secrets/komodo_webhook_secret
      KOMODO_JWT_SECRET_FILE: /run/secrets/komodo_jwt_secret
      KOMODO_FIRST_SERVER_NAME: "{{.Node.Hostname}}"
      KOMODO_OIDC_ENABLED: "true"
      KOMODO_OIDC_PROVIDER: https://auth.authelia.homelab
      KOMODO_OIDC_REDIRECT_HOST: https://auth.authelia.homelab
      KOMODO_OIDC_CLIENT_ID: komodo
      KOMODO_OIDC_CLIENT_SECRET_FILE: /run/secrets/komodo_oidc_client_secret
    secrets:
      - komodo_db_uri
      - komodo_webhook_secret
      - komodo_jwt_secret
      - komodo_oidc_client_secret
    volumes:
      - /mnt/docker-data/infra/komodo/keys:/config/keys
      - /mnt/docker-data/infra/komodo/backups:/backups
      - /mnt/docker-data/infra/komodo/cas:/usr/local/share/ca-certificates
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9120/" ]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname != vm-docker-bragi      
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - databases_internal
      - internet
  
  periphery:
    image: ghcr.io/moghtech/komodo-periphery:2
    init: true
    environment:
      TZ: America/Halifax
      # Use the secure WebSocket path for Komodo 2.x
      PERIPHERY_CORE_ADDRESS: "ws://komodo:9120"
      # Templating IS supported in environment variables
      PERIPHERY_CONNECT_AS: "{{.Node.Hostname}}"
      PERIPHERY_CORE_PUBLIC_KEYS: "file:/config/keys/core.pub"
    volumes:
      - periphery_keys:/config/keys
      # System monitoring mounts
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/proc:ro

      # Core Public Key and Root CA Trust
      - /mnt/docker-data/infra/komodo/keys/core.pub:/config/keys/core.pub:ro
      - /mnt/docker-data/infra/step-ca/certs/root_ca.crt:/usr/local/share/ca-certificates/homelab-root-ca.crt:ro
    deploy:
      mode: global # One container per node
      restart_policy:
        condition: on-failure
        delay: 5s
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
    networks:
      - internet

secrets:
  komodo_db_uri:
    external: true
  komodo_webhook_secret:
    external: true
  komodo_jwt_secret:
    external: true
  komodo_oidc_client_secret:
    external: true

networks:
  databases_internal:
    external: true
  internet:
    driver: overlay

volumes:
  periphery_keys:
