services:
  grafana:
    image: grafana/grafana:12.4
    container_name: grafana
    restart: unless-stopped
    ports:
      - 3000:3000
    env_file: .env
    volumes:
      - /mnt/docker-data/apps/grafana/grafana:/var/lib/grafana
      - ./provisioning:/etc/grafana/provisioning
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - dockernet
      
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.138.0
    container_name: victoria-metrics
    ports:
      - 8428:8428
    volumes:
      - /mnt/docker-data/apps/grafana/victoria-metrics:/storage
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=7d"
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    networks:
      - dockernet
    
  victorialogs:
    image: victoriametrics/victoria-logs:v1.48.0
    container_name: victoria-logs
    ports:
      - 9428:9428
    volumes:
      - /mnt/docker-data/apps/grafana/victoria-logs:/storage
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=7d" 
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    networks:
      - dockernet

  alloy:
    image: grafana/alloy:v1.14.1
    container_name: alloy
    user: "0:0"
    cap_add:
      - SYS_ADMIN
      - SYS_PTRACE
    security_opt:
      - no-new-privileges
    devices:
      - /dev/kmsg
    ports:
      - 12345:12345
      - 4318:4318
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/log:/var/log:ro
      - /etc/machine-id:/etc/machine-id:ro
      - /:/rootfs:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
      - /proc:/proc:ro
      - /mnt/docker-data/apps/grafana/alloy:/var/lib/alloy/data
    entrypoint:
      - /usr/bin/alloy
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=0.0.0.0:12345
    restart: always
    environment:
      - HOST_HOSTNAME=${HOSTNAME}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - dockernet
    depends_on:
      - victoriametrics
      - victorialogs
      
networks:
  dockernet:
    external: true